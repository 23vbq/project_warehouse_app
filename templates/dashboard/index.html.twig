{% extends 'base.html.twig' %}

{% block title %}Dashboard — RimFlowMS{% endblock %}

{% block body %}
<div class="min-h-screen bg-surface-950 text-content-50 flex items-center justify-center">
    <div class="text-center">
        <h1 class="text-2xl font-semibold mb-2">Dashboard</h1>
        <p class="text-content-500 text-sm mb-6">{{ app.user.getUserIdentifier() }}</p>
        <a href="{{ path('app_logout') }}" class="text-sm text-content-500 hover:text-content-300 transition-colors">
            Wyloguj się
        </a>
    </div>
</div>
{% endblock %}
